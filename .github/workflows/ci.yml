name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  swift-test:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Swift Version
        run: swift --version

      - name: Build
        run: swift build

      - name: Test
        run: swift test
